<svg width="300" height="70" xmlns="http://www.w3.org/2000/svg">
  <!-- Background (optional, for visibility) -->
  <rect width="100%" height="100%" fill="white"></rect>

  <!-- Ikon Inventory (kotak bertumpuk) -->
  <g transform="translate(10,15)">
    <!-- Kotak bawah -->
    <rect x="0" y="20" width="40" height="20" fill="#007BFF" rx="2"></rect>
    <!-- Kotak tengah -->
    <rect x="5" y="10" width="40" height="20" fill="#3399FF" rx="2"></rect>
    <!-- Kotak atas -->
    <rect x="10" y="0" width="40" height="20" fill="#66B2FF" rx="2"></rect>
  </g>

  <!-- Teks SIMARSIP -->
  <text x="70" y="42" font-family="Arial, sans-serif" font-size="28" fill="#333">
    SIMARSIP
  </text>
</svg>
